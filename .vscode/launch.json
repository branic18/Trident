{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "extensionHost",
      "request": "launch",
      "name": "Run Extension",
      "outFiles": [
        "${workspaceFolder}/out/**/*.js"
      ],
      "preLaunchTask": "npm: watch"
    },
    {
      "type": "extensionHost",
      "request": "launch",
      "name": "Run Extension (no watch)",
      "outFiles": [
        "${workspaceFolder}/out/**/*.js"
      ]
    }
  ]
}